{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd build && cmake .. && cmake --build .",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}